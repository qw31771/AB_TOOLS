项目 : AB Tools - Maya通用工具插件
环境 : 支持Maya 2022及以上版本
版本 : "1.0.0"
作者 : "abin"
邮箱 : "qw31771@gmail.com"

开发工具工具 : 
pyside2 GUI
maya 2022+
python3
VS Code




## 项目结构
AB_TOOLS/ - 项目根目录
├── install.py - Maya拖放安装脚本，包含onMayaDroppedPythonFile函数，用于将插件路径添加到Maya的sys.path环境变量
├── CLAUDE.md - 项目配置和文档文件，记录项目结构、目标、使用和维护规范
├── base.md - 基础文档模板，包含项目基本信息
├── ab_tools/ - 插件主模块目录，Maya启动后加载的核心插件代码
│   ├── __init__.py - 插件根接口，聚合核心模块（eventManager等）并提供init/run入口
│   ├── main.py - 插件主程序入口，包含工具架创建、脚本启动按钮和UI路由功能
│   ├── config/ - 配置文件目录，包含配置数据和接口类
│   │   ├── __init__.py - 配置接口类（Config）
│   │   └── config_data.py - 配置数据模块，包含路径计算和基础数值定义
│   ├── core/ - 核心模块目录，各模块独立子目录，每个模块自带__init__.py接口
│   │   ├── __init__.py - core路由分发器，聚合各子模块接口
│   │   ├── eventManager/ - 信号大厅（观察者模式/单例）
│   │   │   ├── __init__.py - 信号定义中心：集中注册常量 + 导入时自动注册信号
│   │   │   └── manager.py - 纯事件引擎：EventManager类核心逻辑（无业务信号硬编码）
│   │   ├── preferences/ - 偏好存储（模块化 JSON）
│   │   │   ├── __init__.py - 偏好存储接口，提供 get/set/scope/remove 快捷函数
│   │   │   └── manager.py - 偏好存储引擎：ScopePrefs + PreferencesManager 核心逻辑
│   ├── icons/ - 图标资源目录，用于存放插件所需的所有图标文件
│   ├── prefs/ - 运行时偏好数据目录，存放各模块的 JSON 偏好文件
│   └── ui/ - 界面文件目录，包含PySide2界面实现
│       ├── __init__.py - UI模块初始化文件
│       ├── main_window.py - 主窗口界面实现（纯布局编排）
│       └── AbWidgets/ - 自定义UI组件目录
│           ├── __init__.py - 组件模块初始化文件
│           ├── sidebar.py - 右侧折叠侧边栏控件（抓取条 + 折叠/展开交互）
│           └── statusbar.py - 全局状态栏单例（底部状态文字 + 进度条）
├── tools/ - 工具脚本目录（预留，用于存放可管理的外部脚本）
├── tools_icons/ - 工具图标目录（预留，用于存放工具专用图标）
└── tools_run/ - 工具运行目录（预留，用于存放运行时生成的文件）

---

# CLAUDE.md - AB Tools 项目维护手册

## 1. 项目愿景与目标
* **定位**：Maya 通用工具集成插件。
* **核心目的**：
    * **环境自动化**：通过拖放安装，自动处理 Maya 环境变量（`sys.path`），无需手动配置。
    * **工具集成化**：为外部或自定义脚本提供统一的 UI 管理入口，实现快速启动。
    * **开发高效化**：支持开发者快速将新编写的脚本注册到插件 UI 中，降低工具分发和维护成本。

## 2. 核心架构与逻辑
项目遵循 **“安装 -> 引导 -> 逻辑 -> 表现”** 的解耦架构。

* **安装逻辑 (`install.py`)**：利用 `onMayaDroppedPythonFile` 回调。负责获取当前路径、注入 `sys.path` 并执行 `ab_tools.startup()`。
* **引导逻辑 (`ab_tools/__init__.py`)**：负责模块重载（Reload）策略，确保开发阶段代码修改能即时生效，并调用 `main.py` 的初始化函数。
* **管理逻辑 (`ab_tools/main.py`)**：
    * **自启动逻辑**：在 Maya 顶层控制栏创建 `AB_TOOLS` 工具架。
    * **交互逻辑**：创建 `shelfButton`（包含 `base` 和 `hover` 双态图标）及隔离符。
    * **路由逻辑**：按钮触发后，调用 `ui/` 目录下的接口，不直接包含 UI 布局代码。
* **UI 表现层 (`ab_tools/ui/`)**：基于 PySide2 和 `MayaQWidgetDockableMixin` 实现可停靠的窗口。

## 3. 开发规范 (AI 与开发者必读)

### 🔝 顶级准则 (最高优先级)
0.  **零硬编码**：
    * **所有自变量必须定义在 config 中**：颜色、尺寸、路径、字符串、字体大小等任何可变量，一律在 `config/__init__.py` 对应模块 class 中定义为常量。脚本文件中严禁出现裸数值和裸字符串。
    * **config 中没有就新建**：如果需要的变量在 config 中不存在，必须先添加常量再在脚本中引用。宁可多写一行 config，不留一个硬编码。
1.  **脚本规模与可读性**：
    * **250行上限**：每个 `.py` 脚本不得超过 250 行（含注释和空行）。超出时必须拆分为多个文件。
    * **可读性优先**：代码必须一目了然，变量/函数命名语义化，注释精简有用。
    * **字数最少原则**：能用一行表达的不写三行，优先使用 Python 内置和标准库，避免冗余代码。
    * **规则同步**：每次修改 `CLAUDE.md` 后，必须同步更新 `.claude/skills/project-rules/SKILL.md` 中对应的规范内容，保持两份文件一致。

### ⚠️ 目录与文件约束 (严格准则)
1.  **禁止修改目录结构**：必须保持现有的 `AB_TOOLS/` 根目录及子目录层级。
2.  **增量原则**：允许增加新文件（如在 `tools/` 下增加新脚本），**严禁减少或删除**现有框架文件。
3.  **命名保护**：严禁对现有文件、类名、核心函数名进行重命名。
4.  **模块化要求**：脚本逻辑必须解耦。UI 逻辑严禁出现在 `main.py` 中；核心配置严禁硬编码在 `ui/` 脚本中。
5.  **结构同步要求**：每次项目目录结构发生变化时，必须立即更新 `CLAUDE.md` 文件中的项目结构部分，保持文档与实际结构一致。
6.  **模块初始化规范**：
    * **__init__文件要求**：每个文件夹都必须包含 `__init__.py` 文件，用于提供外部调用内部模块的结构接口。
    * **功能限制**：`__init__.py` 文件只能包含结构调用、固定变量定义和类型声明，严禁进行逻辑运算和复杂计算。
    * **逻辑分离**：所有逻辑运算必须在同文件夹下的其他专门文件中实现，`__init__.py` 仅负责导入和暴露这些功能。
    * **访问控制**：外部代码只能通过 `__init__.py` 导入和访问模块功能，严禁直接导入内部逻辑文件。
    * **接口扩展**：当内部逻辑文件新增需要外部调用的函数时，必须在 `__init__.py` 中添加相应的导入和暴露结构。
    * **文档规范**：`__init__.py` 中的每个导出结构必须包含清晰的文档字符串，说明：
        - 功能描述：该结构是做什么的
        - 参数类型：传入变量的数据类型和格式要求
        - 返回值：输出结果的数据类型和结构说明
7.  **事件管理规范**：
    * **统一事件系统**：项目的所有自定义信号都必须通过 `eventManager` 模块实现和管理（`from ab_tools import eventManager`）。
    * **单例模式**：事件管理器采用单例模式，确保全局只有一个事件管理器实例。
    * **集中注册**：所有信号在 `eventManager/__init__.py` 中集中定义常量并自动注册（导入时执行 `_register_all_signals()`），业务代码无需手动注册。
    * **新增信号流程**：在 `eventManager/__init__.py` 中定义常量 → 加入 `_SIGNAL_REGISTRY` 字典 → 全插件立即可用。
    * **代码维护性**：事件管理器代码应保持通俗易懂、可维护，方便后续添加新事件类型和功能。
    * **线程安全**：事件管理器必须保证线程安全，支持在多线程环境中安全使用。
    * **事件命名规范**：
        - 使用 snake_case 命名风格
        - 前缀表示模块或功能域（如：ui_, tool_, config_）
        - 动词使用过去式表示已完成动作（如：opened, closed, updated）
        - 动词使用现在进行时表示正在进行的动作（如：opening, closing, updating）
    * **使用要求**：
        - 禁止在项目其他地方创建自定义的事件管理系统
        - 所有模块间通信都应通过事件管理器进行
        - 事件处理函数应简洁专注，避免复杂逻辑
8.  **core模块扩展规范**：
    * **物理隔离**：core/ 下的每个功能模块独立为一个子目录，互不干扰。
    * **入口聚合**：新建模块后，在 `core/__init__.py` 中添加导入聚合。
      例：新增 `core/logger/` 模块 →
          ```
          from . import logger
          __all__ = ["eventManager", "logger"]
          ```
    * **根接口提升**：需要对外暴露的模块，在 `ab_tools/__init__.py` 中通过 `from .core import xxx` 提升到根接口。
      例：新增 logger 模块后 →
          ```
          from .core import eventManager, logger
          __all__ = ["eventManager", "logger", "init", "run"]
          ```
    * **调用风格统一**：外部统一通过 `from ab_tools import xxx` 导入，`xxx.功能()` 调用。
      例：`from ab_tools import logger; logger.info("xxx")`
    * **接口与逻辑分离**：每个子目录内的 `__init__.py` 只做结构导入和常量定义，逻辑实现在同目录的其他文件中。
    * **文档同步**：新增模块后同步更新本项目结构部分。


##  操作指南
1.  **安装**：将整个 `AB_TOOLS` 文件夹存放在任意位置，拖拽 `install.py` 到 Maya 视图窗口。
2.  **启动**：点击工具架 `AB_TOOLS` 上的图标。
3.  **开发**：在 `ui/` 目录下编写新界面，在 `main.py` 中注册对应的启动按钮。

### 脚本格式规范
* **编码**：统一使用 `# -*- coding: utf-8 -*-`。
* **环境**：Python 3 (Maya 2022+)。
* **UI 库**：仅限使用 `PySide2`（保持与 Maya 原生库兼容）。
* **路径处理**：使用 `os.path` 进行动态路径获取，禁止使用绝对路径。
* **配置分层规范**：
    * **config_data.py**：仅存放物理路径计算、Maya API 原始值读取、基础数值定义。
    * **__init__.py (Config 类)**：负责将数据组装成逻辑对象。严禁在业务代码中直接 import config_data，必须通过 Config 类访问。
### 配置管理双层准则 (Strict Separation)
1. **数值修改准则**：所有涉及具体的像素数值（如按钮宽、窗口高、边距值）必须写在 `对应文件/__init__.py` 中。
2. **逻辑修改准则**：涉及路径拼接算法、DPI 获取方式、数学计算公式的修改，必须在 ``对应文件/对应文件_data.py`
**严禁**在 逻辑文件中硬编码业务数值。

##  可操作维护记录 (上下文追踪)

### 2026-04-19: 添加 Maya 自启动功能
* **修改目标**：实现插件在 Maya 启动时自动加载，无需每次手动拖拽安装
* **修改文件**：
  * `install.py` - 增加 `setup_user_script()` 函数，在安装时自动检测并创建/更新自启动脚本
* **功能逻辑**：
  * 当用户拖拽 `install.py` 到 Maya 时，自动检查 Maya scripts 目录中是否存在 `userSetup.py`
  * 如果不存在，则创建 `userSetup.py` 并写入 AB Tools 初始化代码
  * 如果已存在，检查是否包含 AB Tools 代码：
    * 如果不包含，则追加初始化代码
    * 如果已包含，则检查 `plugin_dir` 路径是否与当前插件目录一致
    * 如果路径不一致，则自动更新为当前插件目录
* **技术要点**：
  * 使用 `cmds.internalVar(userAppDir=1)+'/scripts'` 获取 Maya scripts 目录路径
  * 使用正则表达式匹配 `plugin_dir = "路径"` 语句，实现路径智能更新
  * 路径比较时进行标准化处理，避免路径分隔符和大小写差异导致的误判
  * 保持与现有安装流程的无缝集成

---